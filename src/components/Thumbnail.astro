---
import { Image } from 'astro:assets'
import { Icon } from 'astro-icon'

interface Props {
  title: string
  price: number
  images: object
  uri: string
}

const { title, price, images, uri } = Astro.props
---

<li class="w-2/4 p-2.5 border-r border-b">
  <a href={uri}>
    <Image
      src={images[0]}
      width="100"
      height="100"
      alt="Product 1"
      class="mb-2"
    />
  </a>
  <a href={uri}><div class="leading-tight mb-1">{title}</div></a>
  <div class="text-2xl font-semibold mb-1.5">
    {price}
    <span class="text-base">₴</span>
  </div>
</li>
